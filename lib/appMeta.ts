export const APP_VERSION = "1.0.0";
export const APP_NAME = "Agriveda";

/**
 * Keep in sync with android/app/build.gradle versionCode / versionName on each release.
 * Force-update uses native versionCode via MIN_NATIVE_VERSION_CODE env (not this string alone).
 */

/** Public farmer support — verify inbox receives mail before Play listing */
export const SUPPORT_EMAIL = "support@agriveda.in";
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
export const PRIVACY_PATH = "/privacy";
export const TERMS_PATH = "/terms";

